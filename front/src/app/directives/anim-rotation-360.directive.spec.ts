import { AnimRotation360Directive } from './anim-rotation-360.directive';

describe('AnimRotation360Directive', () => {
  it('should create an instance', () => {
    const directive = new AnimRotation360Directive();
    expect(directive).toBeTruthy();
  });
});
